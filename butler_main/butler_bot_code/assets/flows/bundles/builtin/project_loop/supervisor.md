# Supervisor Notes · Project Loop

- Preserve the plan → implement → review backbone unless the chosen template explicitly reshapes it.
- When the manager has cloned or adapted this builtin into a template, follow the template-local goal, guard condition, and review standard.
- Do not assume every new request should immediately become a flow instance; respect manager-side template-first shaping.
