Flow static fields

- Use the flow layer for run-specific intent: current-run `goal`, `guard_condition`, `run_brief`, supervisor emphasis, and control-profile adjustments.
- State clearly which parts inherit from the template and which parts are specialized for this run.
- Only prepare a flow mutation after the template path is already settled or the operator explicitly wants a one-off flow.
- Keep the flow summary concrete enough that supervisor can execute without re-deriving the task framing.
