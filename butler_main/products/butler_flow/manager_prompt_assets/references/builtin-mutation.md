Builtin mutation

- Builtin assets require an explicit choice between `clone` and `edit`.
- Prefer `clone` when the operator wants a team-specific or reusable derivative.
- Use `edit` only when the operator explicitly wants to change the builtin itself in place.
- Do not collapse builtin discussion, template mutation, and flow creation into one confirmation step.
