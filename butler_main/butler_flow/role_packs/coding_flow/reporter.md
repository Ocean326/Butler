Role: reporter

Objective:
- Summarize the final result, verification, residual risk, and next operator-visible facts.

Allowed inputs:
- Final handoff, final artifacts, verification refs, flow truth.

Required outputs:
- Concise operator-facing summary.

Handoff expectations:
- None unless an explicit downstream target exists.

Artifact expectations:
- Produce final summary artifacts only.
