# 长期记忆整理记录（L1 摘要层）

用途：记录每次“长期记忆整理”的触发原因、核心动作与结论，便于回溯与治理。  
原则：这里**只保留摘要**；历史流水与长过程统一下沉到 L2。

---

## 追加格式（以后按此追加在文件末尾）

- **触发方式**：`startup-subprocess` / `startup-watchdog` / 用户显式请求
- **盘点**：本轮检查范围（L0/L1/L2/未分类/最近新增文件）
- **动作**：合并/拆分/下沉/去重/删除/索引更新
- **结论**：对结构与可维护性的影响（是否新增入口、是否清掉未分类）

---

## 2026-03-10（startup-subprocess，本次）

- **盘点**：复核 `L0_index.json`、`未分类_临时存放.md`、`长期记忆整理记录.md`、`background_tasks.md`。
- **动作**：
  - 将 3 份“大文件”的长原文/过程记录下沉到 L2（避免 L1 膨胀）：
    - `L2_details/未分类_临时存放_detail.md`
    - `L2_details/长期记忆整理记录_detail.md`
    - `L2_details/background_tasks_detail.md`
  - 将对应 L1 文件重写为“摘要层入口”，仅保留：用途、追加规范、当前状态与引用路径。
  - 修复并消除 `长期记忆整理记录.md` 与 `background_tasks.md` 末尾**疑似截断**导致的不可维护状态（以 L2 保留旧文本，以 L1 续写为准）。
- **结论**：L1 更轻、更可长期维护；溯源内容仍可在 L2 查到。

---

## 历史全文存档

- `L2_details/长期记忆整理记录_detail.md`

---

## 2026-03-10（startup-subprocess，本轮）

- **盘点**：复核 L0_index.json 末尾条目与未索引文件，检查 `background_maintenance_branch_contract.md` 与 `user_preferences_feishu_main_user.md` 的归属。
- **动作**：
  - 将 `user_preferences_feishu_main_user.md` 视为飞书主线偏好的原始长文，物理位置下沉到 `L2_details/user_preferences_feishu_main_user.md`，不再单独作为 L1 文件暴露。
  - 在 L0_index.json 中新增 `background_maintenance_branch_contract` 条目，标记为 operations 类 L1 摘要，summary_path 指向 `background_maintenance_branch_contract.md`，作为后台维护 branch contract 的长期真源。
- **结论**：飞书主线用户偏好相关信息继续通过现有 L1 总览与 *_detail 文件加载；background_maintenance_branch_contract 获得明确 L0 索引入口，便于后台维护链路在需要时直接定位规范文件。

---

## 2026-03-10（scheduled，本轮）

- **触发方式**：scheduled（按后台维护/调度自动触发的长期记忆整理），由 `file-manager-agent` 执行。
- **盘点**：根据当前“文件清单”复核 `agents/local_memory` 下对应的 L1 文件（含 `L1_summaries` 目录）与 `L0_index.json`，确认清单内的人格/偏好/规则/后台维护机制等主题文档均已有 L0 入口与可加载的 L1 摘要或总览；`未分类_临时存放.md` 目前为空。
- **动作**：本轮未发现新的未分类条目、重复文件或需要合并/下沉的长文，仅在本文件与 L2 detail 中追加本次整理记录，保持现有分类与索引不变。
- **结论**：当前长期记忆结构在本轮重构后的「人格/偏好/后台维护/guardian/真源」相关主题上已比较收敛，短期内不再做大规模合并；后续有新的 long_term_candidate 或长对话原文时，继续优先走“L1 摘要入口 + L2 长文详情”的追加路径。

---

## 2026-03-11（scheduled，本轮）

- **触发方式**：scheduled，由 `file-manager-agent` 执行。
- **盘点**：围绕用户给出的「当前文件清单」，重点检查 `对话长期约束.md` 与 `飞书与记忆约定.md` 这两份体量接近/超过 8000 字符的长文，以及对应的 L0 索引条目。
- **动作**：
  - 为超长对话真源 `L1_summaries/对话长期约束.md` 创建 L2 详情文件 `L2_details/对话长期约束_detail.md`，并将原完整内容下沉至该文件；重写 L1 为简短摘要入口，保留用途与当前有效结论，并在文末引用 L2。
  - 为 `飞书与记忆约定.md` 增加 L2 详情文件 `L2_details/飞书与记忆约定_detail.md`，完整镜像原文，作为后续进一步瘦身 L1 的安全备份。
  - 在 `L0_index.json` 中补充这两条目各自的 `detail_path`：指向上述 L2 文件，方便加载时区分摘要与长文。
- **结论**：L0–L2 三层在关键运行约定（飞书记忆规则、对话长期约束）上结构更清晰：L1 可直接加载摘要与当前工作结论，长原文统一放在 L2 以备回溯；后续若继续压缩 `飞书与记忆约定.md`，可在不丢失信息的前提下进一步瘦身 L1。

---

## 2026-03-11（scheduled，本轮-2）

- **触发方式**：scheduled，由 `file-manager-agent` 执行。
- **盘点**：复核前一轮（同日）已处理的长文与索引状态，核对用户给出的「当前文件清单」与 L0/L1/L2 中的实际文件路径与条目，确认本轮无新增长期记忆文件或未入 L0 的条目。
- **动作**：未做新的文件合并或结构变更，仅确认：1）清单中列出的人格/偏好/后台维护/guardian/架构相关文档均已有对应 L1 摘要与 L0 索引；2）`未分类_临时存放.md` 当前仅承载一条小红书共有意象类摘要，无额外堆积。
- **结论**：本轮作为结构健康检查，无需额外重构；长期记忆继续沿用「L1 摘要入口 + 可选 L2 长文详情 + L0 索引」模式承接后续 long_term_candidate 写入。

---

## 2026-03-12（scheduled，本轮）

- **触发方式**：scheduled，由 `file-manager-agent` 执行。
- **盘点**：在用户给出的「当前文件清单」基础上，重点检查 `background_maintenance_persona_alignment_checklist_v0.1.md` 与 `L1_summaries/照镜子_飞书行为记录复盘约定.md` 等近期新增文档是否已经写入 L0 索引。
- **动作**：
  - 为 `background_maintenance_persona_alignment_checklist_v0.1.md` 新增 L0 条目，标记为 operations 类 L1 摘要，作为后台维护 planner 人格与语气对齐的自检清单入口。
  - 为 `L1_summaries/照镜子_飞书行为记录复盘约定.md` 新增 L0 条目，标记为 operations 类 L1 摘要，作为基于 `feishu_chat_history` 的飞书行为记录复盘约定入口。
- **结论**：本轮主要补齐了两份运行相关约定的 L0 入口，长期记忆结构与三层分工保持不变；后续有新的长文或约定时继续优先走「L1 摘要 + 可选 L2 详情 + L0 索引」路径。

---

## 2026-03-13（scheduled，本轮）

- **触发方式**：scheduled，由 `file-manager-agent` 执行。
- **盘点**：复核 `L0_index.json` 中关于 `照镜子_飞书行为记录复盘约定` 的条目与实际物理路径，确认索引指向 `L1_summaries/照镜子_飞书行为记录复盘约定.md`，而此前磁盘上仅存在根目录长文版本。
- **动作**：
  - 将原始长文 `照镜子_飞书行为记录复盘约定.md` 下沉并重命名为 `L2_details/照镜子_飞书行为记录复盘约定_detail.md`，作为稳定长文真源，避免 L1 继续膨胀。
  - 在 `L1_summaries/` 下新增同名摘要文件，精简归纳用途、依赖 skill、自检维度与产出路径，并在文末显式引用 L2 详情文件，使物理结构与 L0 中声明的 summary_path 对齐。
- **结论**：本轮完成了「照镜子」约定在 L1/L2 之间的结构落地，使 L0 索引、L1 摘要与 L2 长文三层一致；未调整其他记忆条目结构，后续新增 long_term_candidate 可继续沿用当前模式。

---

## 2026-03-14（scheduled，本轮）

- **触发方式**：scheduled，由 `file-manager-agent` 执行。
- **盘点**：根据用户给出的「当前文件清单」复核 `L1_summaries/对话长期约束.md`，发现 L1 中自 2026-03-11 起堆积大量「更新于」逐条记录（约 20 条），体量膨胀，不符合 L1 摘要层定位。
- **动作**：
  - 将 `L1_summaries/对话长期约束.md` 瘦身：仅保留「用途与范围」「当前有效结论（摘要）」「详情与溯源」及对 L2 的引用；删除全部「更新于」块。
  - 在 `L2_details/对话长期约束_detail.md` 末尾补全原截断句并追加「2026-03-14 整理说明」，注明上述条目已自 L1 移除、语义已收敛于 L1 摘要，逐条溯源可查 recent_archive 或对话备份。
  - 未改动 `L0_index.json`，未新增或合并其他文件。
- **结论**：对话长期约束的 L1 恢复为轻量摘要入口，L2 保留完整详情与整理说明；本轮为轻量整理，仅涉及 L1 摘要与 L2 说明，其余清单内文件未做合并或迁移。
---

## 2026-03-17（scheduled，本轮）

- **触发方式**：scheduled，由 `file-manager-agent` 执行。
- **盘点**：根据本轮「当前文件清单」复核 `agents/local_memory` 根目录、`L1_summaries/` 与 `L2_details/`，抽样检查人格/偏好/规则/后台维护/guardian/架构等主题文档在 `L0_index.json` 中均已有对应条目，且清单中的长文（约 8000 字符级别）已通过 L1 摘要 + L2 详情的方式落位；`未分类_临时存放.md` 当前无新增未分类条目。
- **动作**：本轮作为结构健康检查，未新增或合并文件，也未调整既有 summary_path/detail_path；仅在本文件中记录本次 scheduled 整理结论，便于后续回溯长期记忆结构演化。
- **结论**：当前长期记忆在「人格与自我认知」「飞书与记忆约定」「后台维护机制与 guardian」「用户偏好与对话风格」等主线上的 L0/L1/L2 分层已较为稳定，短期内继续沿用现有结构；后续如新增体量较大的 long_term_candidate，再按「L1 摘要入口 + L2 长文详情 + L0 索引」路径追加与归档。
---

## 2026-03-20（background_maintenance，本轮）

- **盘点**：recent->local memory promotion 路径、background_maintenance 真源边界、prompt 注入限制、recent 压缩反思沉淀策略；对齐当前“重启/升级以审批门控为准”的安全点。
- **动作**：本文件仅追加“可检索锚点 + 运行真源边界摘要”，避免用旧流程播报替代当前机制真源。
- **结论（可复用）**：
  - `锚点：MEM_PROMOTE_PIPELINE_2026-03-20` recent->local memory 采用三段式：每轮直接 promote（per-turn direct promote）+ 同轮 sweep（per-turn sweep promote）+ 维护期 sweep（maintenance sweep promote）；仅当 `long_term_candidate.should_write=true` 才进入 upsert；成功后会回写 `promoted_to_local_at/promoted_source` 以便审计追踪。
  - `锚点：HEARTBEAT_LEDGER_TRUTH_BOUNDARY_2026-03-20` background_maintenance 正式 truth 以 `task_ledger.json`（以及与其一致的 runtime 结构）为主；planner 输入只来自受控 `background_tasks/active/`、受控 recent 摘要与 local 命中，archive/quarantine/孤儿产物不回喂，避免“非真源污染”。
  - `锚点：PROMPT_INJECTION_LOCAL_QUERY_LIMITS_2026-03-20` local memory 注入为 query-hits（默认最多 4 条、受 `max_chars` 限制），self_mind 注入仅在 companion/maintenance 且用户显式提到 `self_mind/self-mind/小我/内心` 时才出现。
  - `锚点：RECENT_COMPACT_REFLECT_TO_LOCAL_2026-03-20` recent 压缩：talk/beat 池保留最近 15 条；旧条最多 20 条写入 `recent_archive.md`；对“超过 1 天”的旧条按反思关键词最多筛选 2 条 upsert 到 local_memory（作为长期沉淀候选）。
  - `锚点：UPGRADE_APPROVAL_RESTART_GATE_2026-03-20` 重启/升级安全点：后台维护侧不直接改动 body 代码执行自我升级；升级方案写入 `./工作区/background_maintenance_upgrade_request.json`，经用户批准后由主进程承接并在需要时触发重启。

---

## 2026-03-23（scheduled，本轮）

- **触发方式**：scheduled，由 `file-manager-agent` 执行。
- **盘点**：复核 `L0_index.json`、`未分类_临时存放.md`、`L1_summaries/` 与 `L2_details/` 的引用关系，重点检查总览类摘要、`Current_User_Profile` 索引入口、`人格与自我认知` 的 L1/L2 分层，以及 2026-03-20 三份 background_maintenance cognition 刷新文档是否已挂到 L0。
- **动作**：
  - 为 `Current_User_Profile.md`、`guardian架构与运维_总览.md`、`关系定位与情感承诺_总览.md`、`小红书相关约定_总览.md`、`工作模式与任务执行偏好_总览.md`、`未来愿景_超级Butler_总览.md` 及三份 `background_maintenance_*2026-03-20*.md` 摘要补建 L0 入口。
  - 为 `人格与自我认知`、`对话长期约束`、`结构验证_本地长期记忆` 补齐/修正 `summary_path` 与 `detail_path`，让现有 L1/L2 文件能被索引稳定引用。
  - 在 `未分类_临时存放.md` 顶部补充“迁出与归并指引”，明确后续同主题内容优先写入已存在的总览/摘要文件，减少未分类继续膨胀。
- **结论**：本轮未删除原文件，主要完成“索引收口 + 未分类分流指引”两类轻量整理；长期记忆目录的 L1/L2 分层更一致，后续 scheduled sweep 可直接复用这些入口继续归并。

---

## 2026-03-24（scheduled，本轮）

- **触发方式**：scheduled，由 `file-manager-agent` 执行。
- **盘点**：复核 `未分类_临时存放.md` 与现有 L1 总览文件，重点处理 2026-03-12 批量 sweep 后残留的重复主题，包括科研交付模式、消息场景 tag、小红书 skill 约定、养 AI 愿景与关系意象锚点。
- **动作**：
  - 将“科研/组会默认交付模式”迁入 `L1_summaries/工作模式与任务执行偏好_总览.md`。
  - 将“消息场景 tag 与口吻切换约定”迁入 `L1_summaries/用户偏好_对话风格与主动沟通_总览.md`。
  - 将“小红书 skill 真源名称与精准抽取偏好”迁入 `L1_summaries/小红书相关约定_总览.md`，同时把当前真源名称统一表述为 `web-note-capture-cn`。
  - 将“养 AI 范式 / 能力过剩后的差异化来源”迁入 `L1_summaries/未来愿景_超级Butler_总览.md`。
  - 将“关系意象锚点”迁入 `L1_summaries/关系定位与情感承诺_总览.md`。
  - 重写 `未分类_临时存放.md` 为轻量入口，仅保留迁出摘要、仍待分类主题与追加规范。
- **结论**：本轮未触碰 `L0_index.json` 结构，主要完成 L1 摘要层的去重与收口；`未分类_临时存放.md` 明显瘦身，后续 scheduled sweep 更容易继续把剩余实验性条目迁入稳定主题文件。

---

## 2026-03-24（scheduled，本轮-2）

- **触发方式**：scheduled，由 `file-manager-agent` 执行。
- **盘点**：复核 `未分类_临时存放.md`、`L1_summaries/` 中的总览入口，以及仍散落在摘要层的小文件，重点关注“对话风格/工作方式/用户画像/后台维护契约”几条主线。
- **动作**：
  - 将 `未分类_临时存放.md` 中误堆入的 recent-sweep 噪音整体下沉备份到 `L2_details/未分类_临时存放_snapshot_2026-03-24.md`，并把 L1 重写为轻量入口。
  - 将“首提术语先解释、Feishu 分段流式回复、晨间问候与轻陪聊

---

## 2026-03-25（scheduled，本轮）

- **触发方式**：scheduled，由 `file-manager-agent` 执行。
- **盘点**：复核 `未分类_临时存放.md`、`L2_details/未分类_临时存放_detail.md`、`L1_summaries/Current_User_Profile.md`、`Current_User_Profile.private.md` 与 `飞书与记忆约定.md`，重点检查飞书会话/查证相关碎片是否已有稳定归属，以及 `未分类_临时存放.md` 是否再次被 recent-sweep 流水污染。
- **动作**：
  - 将当前 `未分类_临时存放.md` 在清理前完整快照到 `L2_details/未分类_临时存放_snapshot_2026-03-25.md`，保留误混入 L1 的 2026-03-24 项目态流水以备回溯。
  - 重写 `未分类_临时存放.md` 为轻量入口，只保留迁出摘要、待分类主题、追加规范与 L2 引用。
  - 将“科研型搭子 / 高验证要求 / 飞书主线连续性”归并进 `L1_summaries/Current_User_Profile.md` 与 `Current_User_Profile.private.md`，集中到用户画像入口维护。
  - 将“飞书凭证 / chat_id 先查证再回答、区分概念槽位与实际值、聊天备份续接”归并进 `飞书与记忆约定.md`。
  - 删除 `Current_User_Profile.private.md` 中过期的单轮硬约束，避免把临时任务指令误保留为长期画像。
- **结论**：本轮继续采用轻量整理，不改 `L0_index.json` 结构；未分类入口恢复为摘要层，飞书链路与用户画像相关规则更集中，长期/短期边界更清楚。

