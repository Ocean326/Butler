# runs

这里是 `idea_loop` 的实验沉淀层，参考 BrainStorm 的分层治理与任务型项目的 `artifacts/runtime/deliveries` 模式。

## 分层

- `raw/`：原始产物，类似 BrainStorm `Raw`
- `working/`：中间整理稿，类似 BrainStorm `Working`
- `deliveries/`：通过验收的输出，类似任务项目里的 `deliveries`
- `archive/`：归档 run
