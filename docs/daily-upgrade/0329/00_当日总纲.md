# 0329 当日总纲

日期：2026-03-29  
时间标签：0329_0000  
最后修改时间：2026-03-29 23:59:00  
状态：评估完成 / Chat 显式模式拆分与后台任务双状态语义已落代码，其余测试稳定性与配置安全仍需继续收口

## 今日裁决

1. `0329` 的主线不是继续扩功能，而是基于当前仓库、文档、服务状态和关键回归，对 Butler 做一轮现状审计。
2. 当前项目不处于失控状态：
   - `runtime_os` 的分层公开面已经存在
   - `butler_bot / orchestrator / console` 三服务当前均在线
   - runtime / orchestrator / console 的关键抽样回归可以通过
3. 当前最需要优先处理的不是架构方向，而是三类工程风险：
   - 配置中存在明文凭证与 API key
   - `docs/README.md` 与“当天总纲必读”协议已经漂移，`0329` 之前没有当日入口
   - chat 前门相关回归测试存在超时挂住，说明资源清理或环境隔离不完整
4. 当前 Codex provider 默认值已补入系统级主备切换面：
   - 主用 `aixj`
   - 备用 `openai`
   - 熔断超时阈值 `30s`
   - 周期探针 `15min`
   - 冷却窗口 `30min`
5. 当前 live 配置与 `0327` 文档中的“chat 前门临时熔断”口径是一致的：
   - `features.chat_frontdoor_tasks_enabled=false`
6. 当前工作目录缺少 `.git/`，因此本轮只能做“代码 / 文档 / 测试 / 服务状态”四类现状评估，不能对提交历史、脏工作树和真实分支状态做裁决。
7. chat 侧已新增一轮结构性收口：
   - 显式主模式 `chat / share / brainstorm / project / bg`
   - `project` phase `plan / imp / review`
   - `pure` 系列退化为独立 prompt 厚度控制
   - `recent` 改成 mode-specific 配额与 artifact-first 装配
8. 当前 chat 前门总开关仍保持：
   - `features.chat_frontdoor_tasks_enabled=false`
   - 但 `/status`、`/govern` 与显式 `/bg` 不再被当作普通任务前门一起熔断
9. 当前后台任务状态语义已新增一轮结构性收口：
   - 控制面新增 `execution_state / closure_state`
   - query / chat / feedback / console 已统一消费
   - chat 建后台任务主入口仍保持弱化，不在本轮恢复

## 影响的 project-map 条目

1. [当前系统基线](../../project-map/00_current_baseline.md)
2. [真源矩阵](../../project-map/03_truth_matrix.md)
3. [改前读包](../../project-map/04_change_packets.md)
4. [系统级审计与并行升级协议](../../project-map/06_system_audit_and_upgrade_loop.md)
5. [0329 Chat 显式模式与 Project 循环收口](./02_Chat显式模式与Project循环收口.md)
6. [0329 后台任务双状态与前门弱化重构](./03_后台任务双状态与前门弱化重构.md)

## 当前现状评估

### 1. 架构与代码主干

1. `runtime_os` 已按现役口径导出五组命名空间：
   - `agent_runtime`
   - `multi_agent_protocols`
   - `multi_agent_runtime`
   - `durability_substrate`
   - `process_runtime`
2. `multi-agent` 与 `runtime` 主干不是空壳：
   - `runtime_os.multi_agent_runtime`、`runtime_os.multi_agent_protocols`、`runtime_os.durability_substrate` 都已从兼容目录中做公开面收口
3. `manager.py` 已提供统一的服务注册、状态探测、启动与重启入口，当前运维面不是手工散管状态。

### 2. 服务与运行态

1. 本轮执行 `./tools/butler` 对应的 Python manager 状态检查，结果为：
   - `butler_bot: running=True`
   - `orchestrator: running=True`
   - `console: running=True`
2. 其中：
   - `butler_bot` 健康检查返回 `200`
   - `console` 健康检查返回 `200`
3. 这说明当前仓库不是“代码在、服务面断掉”的状态，至少基础运行面已经成立。

### 3. 文档与导航

1. `docs/README.md` 在本轮之前仍把“当前入口”固定在 `0327`，和当前日期 `2026-03-29` 不一致。
2. `docs/project-map/04_change_packets.md` 明确要求所有改动先读“当天 `00_当日总纲.md`”，但 `0329` 目录此前缺失，导致协议存在事实断点。
3. 本轮起，`0329` 这份文档承担当天现状评估真源，用于收口：
   - 当前主干成立点
   - 当前主要工程风险
   - 当前抽样验证结果

### 4. 测试与可回归性

1. 通过的关键抽样回归：
   - `test_runtime_os_namespace.py`
   - `test_runtime_os_root_package.py`
   - `test_orchestrator_workflow_ir.py`
   - `test_orchestrator_runner.py`
   - `test_console_server.py`
   - `test_orchestrator_campaign_service.py`
2. 本轮已确认的通过结果：
   - runtime / orchestrator 第一组共 `22 passed`
   - `test_console_server.py` 共 `4 passed`
   - `test_orchestrator_campaign_service.py` 共 `11 passed`
3. 当前未稳定通过的不是断言失败，而是退出挂住：
   - `test_chat_long_task_frontdoor_regression.py` 在 `timeout 90s` 下已执行 `6` 个用例后未正常退出
   - `test_chat_campaign_negotiation.py` 在 `timeout 90s` 下已执行 `2` 个用例后未正常退出
4. 这意味着 chat 前门链路的测试稳定性仍弱于 runtime / control-plane 主干，后续应优先排查线程、子进程、socket 或事件循环清理问题。
5. 同时，chat 模式拆分 v1 的定向回归已通过：
   - `test_conversation_turn_engine.py`
   - `test_talk_runtime_service.py`
   - `test_agent_soul_prompt.py`
   - `test_chat_recent_memory_runtime.py`
   - `test_talk_mainline_service.py`
   - `test_chat_router_frontdoor.py`
   - `test_request_intake_service.py`
6. 结果：
   - `76 passed`
7. 同时，后台任务双状态语义相关定向回归已通过：
   - `test_orchestrator_campaign_observe.py`
   - `test_orchestrator_feedback_notifier.py`
   - `test_orchestrator_campaign_service.py`
8. 结果：
   - `17 passed`
9. 但 `test_chat_long_task_frontdoor_regression.py` 在本轮复测下仍会超时挂住，说明 chat 建后台任务主路径仍未达到 reopen 条件。

### 5. 当前风险清单

1. `P0` 配置安全风险：
   - `butler_main/butler_bot_code/configs/butler_bot.json` 中当前包含明文 `app_secret` 和 `cursor_api_keys`
2. `P1` 文档治理风险：
   - 当天总纲缺失导致默认阅读协议失效
   - `docs/README.md` 当前入口滞后
3. `P1` 测试稳定性风险：
   - chat 前门回归超时挂住，影响对前门行为的持续验收
4. `P2` 交付形态风险：
   - 当前目录不是完整 git 仓库，无法直接做变更历史和分支治理判断
5. `P1` chat 前门遗留风险：
   - `test_chat_long_task_frontdoor_regression.py` 复测仍挂住
   - 这意味着后台任务虽然已有新的推进 / 闭环双状态语义，但 chat 建任务主入口仍需继续保持弱化

## 本轮验证结果

### 1. 抽样测试

1. 命令：
   - `.venv/bin/python -m pytest butler_main/butler_bot_code/tests/test_runtime_os_namespace.py butler_main/butler_bot_code/tests/test_runtime_os_root_package.py butler_main/butler_bot_code/tests/test_orchestrator_workflow_ir.py butler_main/butler_bot_code/tests/test_orchestrator_runner.py -q`
   - `.venv/bin/python -m pytest butler_main/butler_bot_code/tests/test_console_server.py -q`
   - `.venv/bin/python -m pytest butler_main/butler_bot_code/tests/test_orchestrator_campaign_service.py -q`
   - `timeout 90s .venv/bin/python -m pytest butler_main/butler_bot_code/tests/test_chat_long_task_frontdoor_regression.py -q`
   - `timeout 90s .venv/bin/python -m pytest butler_main/butler_bot_code/tests/test_chat_campaign_negotiation.py -q`
   - `.venv/bin/python -m pytest butler_main/butler_bot_code/tests/test_orchestrator_campaign_observe.py -q`
   - `.venv/bin/python -m pytest butler_main/butler_bot_code/tests/test_orchestrator_feedback_notifier.py -q`
   - `.venv/bin/python -m pytest butler_main/butler_bot_code/tests/test_orchestrator_campaign_service.py -q`
2. 结果：
   - runtime / orchestrator / console 主干抽样通过
   - chat 前门两组回归超时未正常结束
   - chat 模式拆分与 recent/runtime/mainline 定向回归 `76 passed`
   - 后台任务双状态相关定向回归 `17 passed`

### 2. 服务状态

1. 命令：
   - `.venv/bin/python -m butler_main.butler_bot_code.manager status`
2. 结果：
   - `butler_bot` 在线且健康检查 `200`
   - `orchestrator` 在线
   - `console` 在线且健康检查 `200`

## 最终裁决

1. Butler 当前状态应判断为：
   - 架构主线已初步收口
   - 运维主入口可用
   - 关键 runtime / control-plane 主干具备回归基础
   - 但前门测试稳定性、文档同步纪律和配置安全仍未达到稳定工程标准
2. 今日之后若进入升级波次，默认优先级应为：
   - 先清理明文凭证
   - 再修 chat 前门测试挂死
   - 再补文档导航与当日真源同步纪律
3. 本文从今天起作为 `0329` 的现状审计总纲；若后续进入系统级修复，应在同目录继续补 `01_...md` 专题文档，而不是把修复决策继续散落在聊天记录中。
4. `0329` 当天新增的 chat 真源专题为：
   - [0329 Chat 显式模式与 Project 循环收口](./02_Chat显式模式与Project循环收口.md)
5. `0329` 当天新增的后台任务真源专题为：
   - [0329 后台任务双状态与前门弱化重构](./03_后台任务双状态与前门弱化重构.md)
