# 0410 当日总纲

日期：2026-04-10
状态：已落代码 / 已验证 / 当前真源
所属层级：Product Surface（主） / L1 `Agent Execution Runtime`（辅）

## 今日主线

1. 暂时关掉 Butler Desktop 当前前端壳里和这轮目标无关的模式与面板，不再同时追求 runtime、studio、agent detail 等完整工作台叙事。
2. 把 Desktop 前台壳进一步收窄成一个适合增量开发的最小 `Manager shell`：
   - 左侧只保留品牌、`New thread`、真实 history cards
   - 右侧只保留 `Manager` 主对话、消息流和 composer
3. 在不改变 shared surface truth owner 的前提下，为 Desktop 新增最小 manager message 流式 IPC，让右侧主对话具备真实 streaming 感。
4. 保留现有黑/日模式，不把这轮视觉收口做成一次性大改色或新品牌系统。

## 一句话裁决

从今天起，Butler Desktop 当前前台壳的默认开发起点收口为：

> **先用最小 `Manager conversation shell` 建立稳定骨架；其他模式先退后，不抢第一层产品成立。**

补充裁决：

- 左 rail 当前只承担 thread continuity，不再承担产品模式导航。
- 右侧当前只认 `Manager` 为一级对话对象；runtime/studio/agent 先退出主路径。
- `sendManagerMessage` 之外，Desktop 现役新增 `manager-message-stream` IPC，用于右侧主对话的最小流式输出。

## 今日真源

- [0410 Butler Desktop 最小 Manager Shell 与流式对话收口](./01_butler-desktop_最小manager-shell与流式对话收口.md)
- [0409 Butler Desktop Codex 式 Manager Thread 前端升级计划与实施稿](../0409/01_butler-desktop_codex式manager-thread前端升级.md)
- [0403 Butler Flow Desktop 壳与 shared surface bridge 落地](../0403/03_butler-flow_desktop壳与shared_surface_bridge落地.md)
- [真源矩阵](../../project-map/03_truth_matrix.md)
- [改前读包](../../project-map/04_change_packets.md)

## 今日新增结论

1. 这轮 renderer 的正确动作不是继续叠加更完整的 workbench，而是先把现有壳临时收窄。
2. 左侧现役只保留：
   - 品牌区
   - `New thread`
   - 真实 manager thread history cards
3. 右侧现役只保留：
   - 极薄 `Manager conversation` header
   - 基于 manager blocks 组装的消息流
   - 底部 composer
4. 当前无关主路径的 UI 先退出首屏与默认 render tree：
   - `Runtime / Studio` 模式切换
   - snapshot cards
   - runtime/studio strip
   - agent detail sheet
   - code split pane
5. Desktop 继续只经由 `preload + IPC + desktop bridge` 消费 shared surface；这轮没有新增 Desktop 私有 truth。
6. 当前流式对话通过 `manager-message-stream` IPC 实现，允许以最小 started/chunk/completed/failed 事件把最终 response 切片后逐段送入 renderer。
7. 这轮继续把最小壳向 Codex 静态骨架收口：右侧 header 只保留题目 / 工作目录 / 状态，对话列与 composer 同宽居中，composer 常驻底侧，Manager 输出占满列宽、用户输入右对齐约 3/4 宽。
8. manager parse-failed 的原始 Codex 日志当前不再允许直接显示在 Desktop 对话正文里；现役合同是保留 `parse_status / raw_reply / session_recovery` 诊断链，但 `response / summary / latest_response` 统一投影成紧凑失败说明。
9. 当前已确认 parse failure 的根因之一是 manager chat 之前没有像 flow runtime 一样挂 workspace-local `codex_home`，而是直接读取全局 `~/.codex`；当全局 state db migration 不兼容时，会直接把 startup 日志冲进 manager chat。

## 今日完成结果

1. renderer 现已收口成双栏最小骨架：
   - 左 rail
   - 右侧 manager conversation
2. `MissionShell` 已从多模式 workbench 壳改成更克制的 conversation shell。
3. `App.tsx` 已改成最小状态容器，只保留：
   - config attach
   - thread history 切换
   - new thread
   - manager stream send
4. `mission-shell.ts` 已新增 conversation message 归一化 helper，用于把 manager blocks 收成 chat-like 消息流。
5. Desktop 新增流式 IPC：
   - `sendManagerMessageStream`
   - `onManagerMessageEvent`
   - `manager-message-stream` channel/event
6. renderer tests 已更新到新的最小壳口径，并覆盖真实 stream mock。
7. 同日继续把 renderer 再收紧一轮：
   - 左 rail history cards 改成两行轻卡，仅保留题目、时间、状态
   - 右侧 header 改成题目 + 工作目录 + 状态
   - 对话列与 composer 同宽居中，composer 常驻底部
   - Manager 输出与用户消息宽度关系对齐 Codex 风格
8. `manage_agent.py + surface/service.py` 已补 manager parse-failed 正文净化：
   - 新 turn 不再把 raw non-JSON reply 写成用户可见 `response`
   - 旧脏 turn 在 Desktop surface 投影时也会二次净化
9. `manage_agent.py + state.py` 已补 manager chat runtime 隔离：
   - manager chat 现已改用 workspace-local `manage_sessions/_codex_home`
   - 同步带上 flow runtime 同源的 MCP disable overrides

## 今日验收口径

1. 左侧只出现品牌、`New thread`、history cards。
2. 右侧只出现 `Manager` 主对话与 composer。
3. 黑/日模式保持可切换。
4. 发送 manager message 后，右侧能看到 started/chunk/completed 驱动的流式更新。
5. Desktop 现役 bridge/truth chain 未被改成私有真源。
6. 旧 manager thread 中若存在 parse-failed turn，也不应再把 `failed to open state db`、plugin sync/network error 等原始 Codex 日志直接当作聊天正文显示。

## 已执行验证

1. `git diff --check`
2. `cd butler_main/products/butler_flow/desktop && npm run typecheck`
3. `cd butler_main/products/butler_flow/desktop && npm run test:renderer`
4. `cd butler_main/products/butler_flow/desktop && npm run build`
5. `./.venv/bin/pytest butler_main/butler_bot_code/tests/test_butler_flow.py -k parse_failure_preserves_pending_action`
6. `./.venv/bin/pytest butler_main/butler_bot_code/tests/test_butler_flow_surface.py`
7. 重启 `butler_main/products/butler_flow/desktop` Electron Desktop 进行实际复验
