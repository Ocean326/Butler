# 0407 当日总纲

日期：2026-04-07  
状态：脑暴收口 / 文档回写 / 当前真源  
所属层级：跨层理论收口 / docs-only

## 今日主线

1. 围绕 `Codex / Claude Code` 这类 CLI agent 产品，重新审视 “可递归团队契约运行时” 的定位
2. 通过多轮多 agent 讨论、官方资料核对与 `proposal-stress-test`，压实 `team runtime` 与 `team OS` 的边界
3. 在上述边界之上，继续把 `Task + Artifact` 真源、`Team Graph`、`Runtime Kernel` 与 `Workbench Projection` 组合成一份 full-stack 草稿
4. 把最新裁决、`butler-flow -> canonical team runtime` 的删改式升级路径，以及 `docs-only system` 默认并回 `main` 的收口策略一起落成文档，避免继续停留在聊天结论

## 一句话裁决

当前正式口径不是 “直接做 Team OS”，而是：

- 先把 `Codex / Claude Code` 压回 `L1 execution substrate`
- 在其上做 `operator-governed、recoverable、repo-bound` 的 engineering-task runtime
- 把 `task / artifact` 立为交付真源，把 `ownership / authority / policy / receipt` 立为治理真源
- 把 `session / thread` 固定降为 runtime / recovery 容器，`group chat` 固定降为 projection
- 在 `organizational kernel` 被正式对象化并稳定成立前，不过早把这条线命名成完整 `Team OS`

## 今日真源

- [0407 Canonical Team Runtime 最终收口：任务产物真源、治理真源与升级门槛](./01_canonical_team_runtime最终收口_任务产物真源与升级门槛.md)
- [0407 Butler Flow 到 Canonical Team Runtime 升级路径与阶段开发计划（批判式更新版）](./02_butler-flow到canonical_team_runtime升级路径与阶段开发计划_批判式更新版.md)
- [0407 vibe-close：docs 默认并回 `main` 与 worktree 整理策略](./03_vibe-close_docs默认并回main与worktree整理策略.md)
- [CLI Agent Substrate -> Canonical Team Runtime -> Team OS 脑暴收口](../../每日头脑风暴/0407/01_CLI_Agent_Substrate到Canonical_Team_Runtime再到Team_OS_脑暴收口.md)
- [以 `Task + Artifact` 为真源的 Full-Stack Team OS 草稿](../../每日头脑风暴/0407/02_以Task_Artifact为真源的Full_Stack_Team_OS草稿.md)
- [当前系统基线](../../project-map/00_current_baseline.md)
- [系统分层与事件契约](../../runtime/System_Layering_and_Event_Contracts.md)
- [0401 Claude / Codex CLI 单 Session 能力报告](../0401/20260401_claude_codex_cli_session_report.md)
- [0403 Butler Flow Codex 执行根隔离与 `repo_bound` 裁决](../0403/01_butler-flow_Codex执行根隔离与repo_bound裁决.md)
- [0403 Butler Flow supervisor 控制画像与 agents-flow 治理升级](../0403/02_butler-flow_supervisor控制画像与agents-flow治理升级.md)

## 今日新增结论

1. `CLI agent` 是 `execution substrate`，不是 `organizational kernel`
2. `group chat / @ / 1:1 stream` 只能做 projection，不做真源
3. `caller != authority`，human/operator/manager/end-user 必须显式分帽子
4. `v1` 应按单一 proof slice 收窄，而不是先做平台切口
5. 当前更适合的名称是 `canonical team runtime over vendor substrate`
6. 若继续往 full-stack 远景推进，第一真源应立在 `task + artifact`，而不是 `team config` 或 `session/thread`
7. `上级 / 下级` 应保留为关键关系，但只能是 `ownership / authority / spawn` 图的一部分，不再作为唯一组织模型
8. 默认产品入口更适合 `Mission Board + Chat`，工作台负责投影，不负责改写真源
9. 当前 `v1` 只证明 `Receipt-backed Repo-Bound Task Contract Runtime`，不再同时证明完整 recursive team、portable semantics 或 general knowledge work
10. `butler-flow -> canonical team runtime` 的升级路径必须 `subtraction-first`：先冻结 truth owner，再补 contract / receipt / recovery / authority
11. 在 `P6` closure gate 通过前，`team graph` 只能是派生图，`Mission Console` 只能是 projection，不得越级升格成独立真源
12. `docs-only system` 继续按 `system` 严格回写，但默认直接并回当前分支 / `main`，不再自动创建 sibling worktree

## 影响面

- `docs/每日头脑风暴/0407/`
- `docs/daily-upgrade/0407/`
- `docs/project-map/03_truth_matrix.md`
- `docs/project-map/04_change_packets.md`
- `docs/README.md`

## 验收

- 文档内外链与内部相对路径人工检查
- 与现役 `project-map` 分层口径人工对照
