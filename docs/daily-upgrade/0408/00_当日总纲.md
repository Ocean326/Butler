# 0408 当日总纲

日期：2026-04-08
状态：关系澄清 / 分支对齐裁决 / 文档回写 / 当前真源
所属层级：L1 `Agent Execution Runtime` / L2 durability substrate / Product surface

## 今日主线

1. 把前台 `butler-flow` 里 `team runtime` 与 `desktop shell` 的关系从口头共识收口成现役文档。
2. 对齐当前仓库里两条已经真实存在、但仍分落不同分支的线：
   - `main@80d595b`：`canonical team runtime over vendor substrate` 的 runtime 闭环基线
   - `codex/flow-desktop@ae31396`：Desktop thread-first workbench 的产品壳与 bridge 契约
3. 明确当前主线不是“扩一个更大的 Team OS”，也不是让 Desktop 先自成平台，而是先把 Desktop 重新压回 projection 配套角色，再完成与 runtime 基线的合流。
4. 把这轮裁决同步回写到 `project-map` 与 `docs` 入口，避免后续 agent 继续把 Desktop 和 runtime 主体混为一谈，或误以为当前分支已经带有 `0407` 全量闭环代码。

## 一句话裁决

当前正确口径是：

- `team` 是前台 `butler-flow` 上的 runtime 语义
- `desktop` 是该 runtime 的 projection shell
- 接下来的主线应是：
  - 先把 Desktop 线对齐到 `main@80d595b` 的 canonical runtime 基线
  - 再完成 `P6 Closure Gate`
  - 最后再决定 Desktop 是否进入下一阶段产品化

## 今日真源

- [0408 Team 与 Desktop 关系、当前进度与下一条主线](./01_team与desktop关系_当前进度与下一条主线.md)
- [0405 Butler Flow Desktop 线程化工作台与 Manager-Supervisor 串联落地](../0405/01_butler-flow_desktop线程化工作台与manager-supervisor串联落地.md)
- [0403 Butler Flow Desktop 壳与 shared surface bridge 落地](../0403/03_butler-flow_desktop壳与shared_surface_bridge落地.md)
- [真源矩阵](../../project-map/03_truth_matrix.md)
- [改前读包](../../project-map/04_change_packets.md)
- 对接基线：`main@80d595b` 上的 canonical runtime 闭环提交与其 `0407` 文档组

## 今日新增结论

1. 当前仓库里的 `team` 不是 `Team OS`、不是可写 `team graph`，也不是后台 `campaign/orchestrator` 的别名。
2. `team runtime` 的目标语义已经在 `main@80d595b` 上被收口为 canonical runtime 基线，但当前 `codex/flow-desktop` 分支尚未把这套对象和文档带进来。
3. Desktop 当前已经是可编译、可桥接、可测试的真实工程壳，但它只负责读取 shared surface，不负责定义 truth owner。
4. 当前 repo 的真实状态不是“一条线全部完成”，而是两条线尚未合流：
   - `main@80d595b` 已完成 runtime 闭环基线
   - `codex/flow-desktop@ae31396` 已完成 Desktop thread-first 壳与 bridge
5. 因此接下来的关键不是继续扩对象，而是先让 Desktop 回到 projection 配套角色，并把这条分支重新对齐到 runtime 基线。
6. 只有完成合流并通过 `P6` gate 后，才值得讨论 Desktop 自动刷新、更多 operator UX、packaging/release 等产品化话题。

## 当前进度摘要

1. Runtime / Team 线：
   - `main@80d595b` 已形成 canonical runtime 闭环基线
   - `P0-P5` 已在主干线完成
   - `P6` 仍是 gate，不是新功能阶段
   - 但这些对象与 `0407` 文档当前不在本分支工作树中
2. Desktop 线：
   - `codex/flow-desktop@ae31396` 已有壳、bridge、shared surface 与 thread-first renderer 契约
   - 已有 `typecheck + vitest + bridge/e2e` 基础验证
   - 但当前代码里仍搜不到 `task_contract / receipts / recovery_cursor` 这类 `0407` runtime 真源对象
   - 因此它仍未完成与 canonical runtime 基线的收束

## 下一条主线

1. 主线 A：先做 Team/Desktop 合流
   - 先把 `codex/flow-desktop` 对齐到 `main@80d595b`
   - 目标是让 runtime truth 和 Desktop projection 重新回到同一条事实链
2. 主线 B：合流后完成 `P6 Closure Gate`
   - 继续按 `G0-G5` 做 runtime 闭环证据
   - Desktop 重点承担 `G5 projection consistency`
3. 主线 C：Desktop 只做 `P6` 所需 projection hardening
   - 真实 workspace fixture
   - real payload 一致性
   - contract/receipt/recovery 摘要稳定展示
   - artifact open / toast / preflight / settings 最小闭环
4. 主线 D：只有 `P6` 通过后，再决定 Desktop 是否进入产品化

## 影响面

- `docs/daily-upgrade/0408/`
- `docs/project-map/03_truth_matrix.md`
- `docs/project-map/04_change_packets.md`
- `docs/README.md`

## 验收

- 文档内链与相对路径人工检查
- 与 `0407` runtime 真源、`0405` desktop 真源、当前代码现状人工对照
- `git diff --check`
