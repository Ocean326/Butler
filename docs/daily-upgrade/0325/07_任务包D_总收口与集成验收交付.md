---
type: "note"
---
# 07 任务包 D 总收口与集成验收交付

日期：2026-03-25
时间标签：0325_0007
状态：已迁入完成态 / 第一阶段收口回执

## 这次 D 实际收了什么

D 这轮不再做“普通跟随”，而是把 `0325` 的总验收口直接落到同一组代码和测试上：

1. consumer 侧开始统一改用 `runtime_os` 兼容命名空间，停止在 `orchestrator` 继续扩散 `agents_os / multi_agents_os` 旧名。
2. `workflow_ir.execution_boundary()` 不再只暴露旧 owner 字符串，而是同时给出：
   - 当前 control plane owner
   - 当前 selected engine
   - `runtime_os.agent_runtime / runtime_os.process_runtime`
   - 兼容期 `agents_os / multi_agents_os`
   - `governance_target_owner`
3. `query / observe` 增加 `closure_signals` 聚合视图，直接回答总验收最关心的几件事：
   - `workflow_ir_compiled` 是否可见
   - `workflow_vm_executed` 是否可见
   - `workflow_session_count`
   - session-aware branch 数量
   - governance 事件可见性
   - package binding 可见性
4. `smoke` 输出补齐 `execution_boundary / gate_policies / package_binding_visible`，让 demo 不再只证明“跑通一次”，而能证明命名、runtime、governance 归属一起可观测。

## A/B/C 复核后的 Closure Checklist

### A：命名迁移与兼容期

- [x] `runtime_os` 作为新总名继续冻结。
- [x] `orchestrator` consumer 侧开始跟随新命名空间。
- [x] `smoke / observe / tests` 可看见新命名边界。
- [x] 兼容壳仍保留，未阻断现有旧 import。
- [ ] 物理目录 rename 与大范围 codemod 仍未完成。

### B：process runtime 收口

- [x] 普通 path 已有 session-aware 证据链。
- [x] `workflow_session_count > 0` 已能在 `query / observe` 直接看到。
- [x] package refs 已能在 `workflow_ir / smoke / closure_signals` 看到。
- [x] `approval / verification / recovery` 事件已能在 session 与 event window 看到。
- [ ] governance 真执行层仍主要在 `orchestrator/service.py`，尚未完全迁回 `runtime_os.process_runtime`。

### C：orchestrator control plane 收缩

- [x] `workflow session bridge` 已从 `service.py` 抽离。
- [x] `execution_bridge / runtime_adapter / workflow_vm / query / observe / smoke` 已形成同一条可验收链。
- [x] `runtime verdict -> writeback -> observe` 已稳定到现有测试口径。
- [ ] research/domain 文件仍留在 `orchestrator` 目录内，尚未完成降层。

## D 已补的集成改动清单

1. `orchestrator` 相关 consumer import 跟随到 `runtime_os`：
   - `execution_bridge.py`
   - `runtime_adapter.py`
   - `runner.py`
   - `service.py`
   - `runtime_bridge/workflow_session_bridge.py`
   - `mission_orchestrator.py`
   - `research_bridge.py`
2. `workflow_ir` 的 execution boundary 从旧 namespace 升级为：
   - `runtime_namespace`
   - `agent_runtime_owner`
   - `process_runtime_owner`
   - `governance_target_owner`
   - `compat_execution_owner`
   - `compat_collaboration_owner`
3. `query / observe` 新增 `closure_signals` 聚合输出。
4. `smoke` 输出补齐 boundary、gate policy、package binding 证据。
5. `paths.py` 接受 `butler_main/runtime_os` 作为合法 workspace 锚点。

## 本次补齐后的第四层收口

除了前面已经收掉的命名、runtime、observe、smoke 证据链之外，本次还把第四层消费面正式补成了独立 contract，而不是继续散落在 query 返回值约定里：

1. 新增 `butler_main/orchestrator/fourth_layer_contracts.py`，冻结：
   - `FOURTH_LAYER_PORT_NAMESPACE`
   - `FOURTH_LAYER_PORTS`
   - `FOURTH_LAYER_STABLE_EVIDENCE_KEYS`
   - `FOURTH_LAYER_FORBIDDEN_DIRECT_IMPORTS`
2. `query_service.py` 现在会统一补出：
   - `mission_view`
   - `branch_view`
   - `session_view`
   - `build_observation_snapshot()` 生成的 observation contract
3. `orchestrator.__init__` 已导出第四层 contract 的稳定入口。
4. `08_第四层接口冻结_V1_简化版.md` 现在和 D 包形成前后对应：
   - `07` 负责总收口与验收口径
   - `08` 负责第四层 contract 冻结件

## 必须更新的 Smoke / Observe / Query / Fixture / Tests

### Smoke

- 已更新 `butler_main/orchestrator/smoke.py`
- 新增输出：
  - `workflow_ir.execution_boundary`
  - `workflow_ir.gate_policies`
  - `workflow_ir.package_binding_visible`

### Observe / Query

- 已更新 `butler_main/orchestrator/service.py`
- `build_observation_window()` 现在返回 `closure_signals`
- `observe window` 可直接看：
  - runtime namespace
  - required vs observed event types
  - workflow session count
  - governance event types
  - package-bound branch count

### Fixtures

- 继续沿用已冻结的两条 demo fixture：
  - `superpowers_like`
  - `openfang_inspired`
- 不再新增第三条临时 demo，避免 D 再次发散

### Tests

- 已更新：
  - `test_orchestrator_smoke.py`
  - `test_orchestrator_runner.py`
  - `test_orchestrator_ingress_and_template.py`
  - `test_orchestrator_workflow_ir.py`
  - `test_orchestrator_workflow_vm.py`
  - `test_orchestrator_framework_compiler.py`

## 截至当前主干的最终验收

1. 与本轮收口直接相关的测试当前共验证 `45` 项，全部通过。
2. 这意味着一阶段现在可以正式结案为：
   - `runtime_os` 命名空间成立
   - `process runtime` 表面成立
   - `orchestrator governance bridge` 已抽出
   - 第四层 contract 与统一证据集成立
3. 以下事项不再算 D 包未完成，而是明确转入第二阶段：
   - `agents_os / multi_agents_os` 物理 rename 与 codemod
   - governance 真执行层继续回迁到 `runtime_os.process_runtime`
   - `orchestrator` 物理目录分层
   - 普通 path 进一步走向多步、resume、wait、gate 的真实 workflow

## 可后置但不阻塞总验收的部分

1. `agents_os / multi_agents_os` 物理目录 rename
   - 原因：已有 `runtime_os` 兼容壳和 codemod 草案，今天不阻塞总验收链。
2. `approval / verification / recovery` 真执行层彻底回迁到 `runtime_os.process_runtime`
   - 原因：当前已通过 `gate_policies + execution_boundary + event window` 把“当前 owner / 目标 owner”说清，且 demo / smoke / observe 已可验收。
3. `research_bridge.py / research_projection.py` 从 `orchestrator` 降层到 domain 目录
   - 原因：普通 path 的闭环已打通，这项不阻塞 `0325` 总验收。

## Done 定义

之所以判定 D 可以收口，是因为 `0325` 要的不是“每个包各有方案”，而是同一条可验证闭环已经成立：

1. framework profile 能进入 compiler，并落成 Butler Workflow IR。
2. 普通 path 已真实创建并消费 workflow session。
3. workflow VM 执行结果已回写 mission / node / branch。
4. `smoke / query / observe / tests` 能同时看到：
   - `workflow_ir_compiled`
   - `workflow_vm_executed`
   - `workflow_session_count > 0`
   - package binding
   - approval / verification / recovery 中至少一类 governance 事件
5. A/B/C 的尾项没有继续散落在各自文档里，而是被收成单一 closure checklist，并区分了“已完成”和“可后置但不阻塞总验收”。

到这个点，`0325` 这条总任务已经具备明确的收口位；后续剩余项属于下一轮演进，不再属于当天必须新增的“总收口包”。
