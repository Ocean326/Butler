# 0316 执行结果：self_mind 解耦与 heartbeat 治理

## 已完成

- `self_mind` 改为对 talk-heartbeat 只读，不再接受对话轮次或 heartbeat 快照反写。
- 关闭旧 `bridge` 实际写通路；`mind_body_bridge.json` 不再作为现役执行真源。
- `self_mind` 的重动作改为留在 `./工作区/03_agent_upgrade/self_mind_agent_space/` 的私有执行空间，不再写入 heartbeat 看板或 task ledger。
- `self_mind` prompt 从 `talk|heartbeat|hold` 收敛为 `talk|agent|hold`。
- heartbeat 固定代谢支路更新为“只做 still-valid 的轻量治理”，明确不再维护 guardian 旧巡检、self_mind 配额实验等过时任务。
- 清理了 `heartbeat_tasks.md`、`05_整理清洁.md`、`06_闲时无聊.md` 中一批过时任务，并把相关项改为 `obsolete`。
- 补充了 `工作区/`、`04_heartbeat/`、`06_governance_ops/`、`07_skills/`、`03_agent_upgrade/`、`self_mind_agent_space/`、`heartbeat_tasks/` 的 README 规则。
- 运行配置已调整为 `workspace_root="."`，`heartbeat.max_parallel_branches=4`。
- 服务已重启，当前主进程恢复运行。

## 验证

- 针对性测试：
  `butler_main/butler_bot_code/tests/test_self_mind_services.py`
  `butler_main/butler_bot_code/tests/test_memory_manager_recent.py`
  `butler_main/butler_bot_code/tests/test_heartbeat_orchestration.py`
- 结果：`69 passed`

## 剩余观察点

- `memory_manager.py` 仍然偏重，这轮主要做的是切桥、收边界、稳行为，没有完成更深的职责拆分。
- `self_mind` 现已从 heartbeat 任务链解耦，但它自己的 `agent_space` 还只是边界与目录就绪，后续可以再接独立执行模板或索引。
- 工作区历史遗留目录仍然很多，README 已先立规，后续需要按编号目录继续迁移和归档。
