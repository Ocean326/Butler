# chat 升级具体实施计划（今日）

## 一、今日目标

今天的目标不是完成全部 chat 重构，而是把本轮系统性升级推进到“方向清晰、代码有抓手、下一步可直接执行”的状态。

## 二、今日已完成

### 已落代码

- 微信长轮询 timeout 边界修复
- 微信本地地址绕过系统代理
- 微信官方登录二维码与本地页链路修复
- 微信回复纯文本归一化，去飞书 Markdown / 卡片味
- 飞书 / 微信 / CLI 对话提示词拆分
- 统一 `build_chat_agent_prompt`
- 本地常驻 `chat core`
- 本地 `butler` / `butler-core` 包装命令

### 已落文档

- 本目录全部升级文档

## 三、今日剩余建议动作

### 动作 A：把 `channel_profiles.py` 拆成策略对象的第一步

建议今日完成到：

- 定义 `ChannelPromptPolicy` / `ChannelPresentationPolicy` 协议
- 先为 `feishu / weixin / cli` 各建一个 policy 文件
- `channel_profiles.py` 退化成数据和兼容层

预期产出：

- 公共层 if/else 明显减少

### 动作 B：引入最小 `channel registry`

建议今日完成到：

- registry 能按 channel key 返回对应 policy
- `prompting.py` 和 `runtime.py` 先改成读 registry

预期产出：

- 为后续从 `channel_profiles.py` 迁出行为逻辑铺路

### 动作 C：补 core 运维命令最小闭环

建议今日完成到：

- `butler status`
- `butler stop`

预期产出：

- 本地常驻 core 可控

## 四、今日不建议硬做的动作

以下动作不建议今天硬做完：

- 直接大迁移到 `chat/channels/`
- 一次性把 `mainline.py` 完全去掉飞书 / 微信 import
- 做群聊 / 多 participant 语义
- 做远程 API 服务化

原因：

- 改动面过大
- 回归面过宽
- 容易把今天的升级收口重新打散

## 五、具体实施顺序

### Step 1

完成渠道 policy 协议定义与三份实现骨架。

### Step 2

把 `prompting.py` 的渠道 prompt 逻辑改走 policy。

### Step 3

把 `runtime.py` 的 output normalization 改走 presentation policy。

### Step 4

补回归测试：

- prompt policy
- presentation normalization
- registry resolve

### Step 5

补 `butler status / stop`。

## 六、今日完成标准

今天结束前，如果达到以下状态，就算本轮升级推进有效：

- 文档体系已建立
- core / butler 已可工作
- 微信 / 飞书 / CLI 对话提示词已拆开
- 下一步代码升级已聚焦到“拆 `channel_profiles.py`”

## 七、下一工作日建议续做项

1. 完成 policy 对象化。
2. 引入 channel registry。
3. 把 `mainline.py` 改成 registry 驱动。
4. 评估是否正式迁移到 `chat/channels/`。
