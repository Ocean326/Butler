# 0331 当日总纲

日期：2026-03-31  
时间标签：0331_0000  
最后修改时间：2026-04-01  
状态：已落代码 / 前台 butler-flow CLI（历史名 workflow shell）与 Codex CLI 监管适配、当日运行入口修复，以及 `0330/02` Agent Harness 真源拆分回写；晚间补两份长任务方向草稿计划；夜间再补 launcher 与 MCP 隔离修复；随后再收口为“默认继承本机 Codex 配置”；并完成 04b-butler-flowV1 第一轮实现（supervisor/run-turn-action-rich-shell）；补一轮系统级 CLI 入口安装面；再补一轮编码容错与外部 CLI 对标计划刷新；最后完成 04c Textual-first CLI 完备升级实现；当日末补 `/flows` `/manage`、`managed_flow`、`flow_definition.json` sidecar；随后再落实 06 前台 role runtime / role-session 绑定，版本升至 `1.2.0`

## 执行前快速摘要

- 今日变更可先按四组理解：`butler-flow` 前台 CLI 产品化、`campaign` 主线瘦身、`visual console` 修复与对齐、文档导航/根目录治理回写。
- 代码主落点优先看：`butler_main/butler_flow/`、`butler_main/domains/campaign/`、`butler_main/orchestrator/`、`butler_main/console/`、`tools/`。
- 今日关键稳定性修复有两条：`tools/butler status` 循环导入修复；`/console/` 静态资源基路径修复。
- 今日新增执行面能力重点是：`butler-flow` 的 `run/resume/status/list/flows/preflight/action/exec/tui`、`/manage` 管理代理协议交接、`managed_flow` 有序 phase plan、`flow_definition.json` sidecar、独立 `codex_home/`、Textual TUI、结构化事件脊柱与 operator action 合同。
- 今日追加的前台运行时重点是：`execution_mode=simple|medium|complex`、`role_pack=coding_flow|research_flow`、`role_sessions.json`、`handoffs.jsonl`、role-bound prompt 装配与 artifact visibility。
- 今日架构裁决重点是：前台 `butler-flow` 继续留在 L1，不进入后台 `campaign/orchestrator`；后台新主线收口为 `campaign 宏账本 -> workflow_session -> latest_turn_receipt/task_summary`。
- 下一阶段若要复验，优先跑：`test_butler_flow.py`、`test_butler_cli.py`、`test_console_server.py`、`test_console_services.py`、`test_orchestrator_campaign_service.py`、`test_orchestrator_campaign_observe.py`、`test_orchestrator_feedback_notifier.py`、`test_runtime_os_root_package.py`、`test_butler_manager.py`。

## 今日裁决

1. 归档一份 **宿主 Agent（如 Cursor）监管 Codex CLI** 的可重复做法：何时用 `exec`、非交互 `resume`、重试与超时、`relay` profile 与 MCP 鉴权边界。
2. 专题正文（真源）：[01_Agent监管Codex实践_exec与resume.md](./01_Agent监管Codex实践_exec与resume.md)
3. Butler 新增 **前台 butler-flow CLI**（历史名 `workflow shell`），固定收口为 L1 `Agent Execution Runtime` 的前台附着 CLI workflow：
   - `tools/butler-flow run`
   - `tools/butler-flow resume`
   - `tools/butler-flow status`
   - `python -m butler_main.butler_flow`
   - 旧入口 `butler workflow` / `butler -workflow` / `codex-guard` 已退役为迁移提示，不再执行
4. 前台 workflow shell 真源正文：[02_前台WorkflowShell收口.md](./02_前台WorkflowShell收口.md)
5. 当日补一条实际代码修复：
   - `tools/butler status` 因 `manager.py -> chat.core -> config_runtime -> runtime_os.agent_runtime -> agents_os.execution -> workflow_shell -> chat.config_runtime` 形成循环导入而失效
   - 已改为轻量依赖：
     - 新增 `butler_main/chat/core_defaults.py`
     - `manager.py` / `core_client.py` 不再顶层 import `chat.core`
     - `config_runtime.py` 直接依赖 `agents_os.execution.logging.set_runtime_log_config`
   - 验证已完成：
     - `tools/butler status`
     - `test_butler_manager.py`
     - `test_runtime_os_root_package.py`
6. 当日还完成一轮 docs-only 真源拆分：
   - [0330 Agent Harness 全景研究与 Butler 主线开发指南](../0330/02_AgentHarness全景研究与Butler主线开发指南.md) 现在只保留总裁决与总导航
   - 分层子计划真源已拆到 `02R/02A/02B/02C/02D/02F/02G`
   - 已同步回写 `docs/README.md`、`project-map/02_feature_map.md`、`project-map/03_truth_matrix.md`、`project-map/04_change_packets.md`
7. 当日晚些时候补一条 visual console 可访问性修复：
   - 症状：`8765` 返回的 `/console/` HTML 可打开，但构建产物把静态资源写成根路径 `/assets/...`，导致浏览器实际加载 JS/CSS 404，表现为“本机和内网都打不开/白屏”
   - 修复：
     - `butler_main/console/webapp/spa/vite.config.ts` 增加 `base: "/console/"`
     - `butler_main/console/server.py` 对 `index.html` 增加 `/assets/ -> /console/assets/` 的兼容重写
     - `test_console_server.py` 增加静态资源路径回归测试
   - 复验：
     - `http://127.0.0.1:8765/console/`
     - `http://10.134.142.90:8765/console/`
     - `http://10.254.73.179:18766/console/`
     - `http://127.0.0.1:8765/console/assets/index-*.js`
8. 当日晚些时候原先补了一份“后台主线控制面瘦身与 Agent 内环提权”草稿；本轮已把它落为实际代码并回写成现役专题真源：
   - 专题正文：[03_后台主线控制面瘦身与Agent内环提权草稿计划.md](./03_后台主线控制面瘦身与Agent内环提权草稿计划.md)
   - 当前现役裁决：
     - campaign 主线改为 `campaign 宏账本 -> workflow_session 内环状态 -> agent turn receipt -> harness 持久化/恢复/设施`
     - `resume_campaign` 对新 campaign 固定为“一次可重入 supervisor turn”
     - 新 campaign 不再创建 `mission/node/branch`，`mission_id` 默认允许为空
     - 对外稳定面收口到：`campaign_id`、`canonical_session_id`、宏 `status`、`task_summary`
     - `feedback/query` 优先读取 `task_summary`、`latest_*` 与 `latest_turn_receipt`
9. 当日晚些时候再补一份**未实施草稿计划**，聚焦“前台长 Agent 监督 Workflow 产品化”：
   - 草稿正文：[04_前台长Agent监督Workflow产品化草稿计划.md](./04_前台长Agent监督Workflow产品化草稿计划.md)
   - 当前定位：
     - 不是把现有 `workflow shell` 直接提升成后台主链
     - 而是为前台长任务新增一条 `Foreground Workflow Run` 轻主线
   - 草稿主结论：
     - 让 agent 成为前台 workflow 的主驱动者
     - 但通过 `thread/turn/item`、approval、receipt、run history、handoff boundary 保留 harness 稳定性
10. 夜间补一轮 **前台 workflow shell CLI 升级与 bug 修复**：
   - `butler -workflow` / `butler workflow` 在交互终端下无子命令时，改为直接进入 launcher / 向导，而不再只打印 help
   - launcher 默认建议 `project_loop`，并展示最近 workflow、preflight 与 resume/status 快捷动作
   - 后续再收口一轮默认行为修正：
     - workflow shell 调 Codex 时，默认不再隐式注入 `--profile`
     - 随后又补一条运行时修正：在 workflow-local `codex_home` 下，默认对 `stripe / supabase / vercel` 追加局部 MCP 禁用覆盖，避免 OAuth 型远程 MCP 在隔离执行态内触发 `AuthRequired` / `rmcp::transport::worker` 卡死
     - 当前默认仍继承本机 `codex` 的 profile / provider / auth 基础配置；若要关闭这层 guard，需显式配置 `workflow_shell.disable_mcp_servers: []`（现役键同名迁到 `butler_flow.disable_mcp_servers`）
     - 同时为每条 workflow 准备独立 `codex_home/`，同步最小必要的 Codex 配置/认证文件，但不继承全局 MCP 注册状态
     - `Ctrl+C` 中断时，workflow 固定写成 `interrupted` 并返回 `130`，不再抛 Python traceback
    - `project_loop` 的 `phase_attempt_count` 改为只统计 **Codex 成功完成** 的同 phase 尝试；纯中断 / timeout / rmcp 卡死导致的失败不再直接耗尽 phase 预算
    - 这让已有失败的 `plan` workflow 在修复后可继续 `resume`，不必因历史 timeout 记录而永久卡死
11. 按 [04b-butler-flowV1版本开发计划](./04b-butler-flowV1版本开发计划.md) 完成 V1 第一轮实现并回归：
    - runtime 显式引入 supervisor decide，形成 `supervisor -> executor -> judge -> optional recovery -> turn finalize` 环
    - 新增持久化面：`turns.jsonl`、`actions.jsonl`、`artifacts.json`
    - 新增 operator action 合同与 CLI：
      - `butler-flow action --type pause|resume|append_instruction|retry_current_phase|abort`
    - 新增 TTY 轻量 `RichFlowDisplay`，非 TTY 路径保持不变
    - 保持旧 `flow_state.json` 兼容读取并迁移写回 `workflow_state.json`
    - 验证：
      - `./.venv/bin/python -m pytest butler_main/butler_bot_code/tests/test_butler_flow.py butler_main/butler_bot_code/tests/test_chat_cli_runner.py butler_main/butler_bot_code/tests/test_butler_cli.py -q`
      - 结果：`29 passed`
12. 再补一条系统级 CLI 入口收口：
    - 新增 `tools/install-butler-flow`
    - 安装后系统 PATH 中的 `butler-flow` 指向仓库内 `tools/butler-flow`
    - 新增 `python -m butler_main` 顶层入口，默认落到 `butler-flow`
    - 用于把 `butler-flow` 从“仓库内脚本名”提升为“可直接敲的系统级 CLI 命令”
13. 再补一条前台 CLI 稳定性修复：
    - `provider_failover.sync_system_codex_profile` 读取 `~/.codex/config.toml` 改为容错 UTF-8
    - 用于避免历史脏编码或坏字节把 `butler-flow` 直接炸成 `UnicodeDecodeError`
    - 同时把 launcher / run / resume 的 `Ctrl+C` 收口为退出码 `130`，不再抛 Python traceback
14. 再补一轮外部 agent CLI 对标刷新：
    - 回写 [04a_交互式CLI对标调研与ButlerFlow升级方案.md](./04a_交互式CLI对标调研与ButlerFlow升级方案.md)
    - 当前重点对标对象更新为：`Codex CLI`、`Claude Code`、`OpenCode`、`Gemini CLI`、`Goose`、`aider`
    - 结论是：Butler 先补 `system CLI entry + stable event spine + operator TUI shell`，不先追“嵌套 vendor TUI”
15. 再补一轮 **Textual-first CLI 完备升级主计划**：
    - 新增正文：[04c_butler-flow完备升级与视觉设计计划.md](./04c_butler-flow完备升级与视觉设计计划.md)
    - 当前裁决：
      - `Python runtime + Textual shell + serializable event spine`
      - 吸收 Codex / Claude 类 CLI 的 fullscreen、raw input、slash commands、hooks、permissions、subagent events
      - 不照搬 Rust / Ink / Node 技术栈，不嵌套 vendor 原生 TUI
      - 当前先做 Textual 主壳，同时为 future native/Ink shell 预留 controller / event 复用位
      - `Textual` 依赖单独落位到 `requirements-cli.txt`，不并入 `requirements-dev.txt`
16. 再把 **04c Textual-first CLI** 从计划落实到代码：
    - 新增 `requirements-cli.txt`
    - 新增 `butler_main/butler_flow/events.py`
    - 新增 `butler_main/butler_flow/tui/`
    - `butler-flow` 在交互终端无子命令时优先进入 Textual launcher
    - 新增 `butler-flow tui`
    - `run` / `resume` 在交互终端默认进入 attached run screen，`--plain` 保留旧路径
    - `FlowRuntime` 已接入 `FlowUiEvent`
    - TUI 已接入 slash commands、confirm-based operator action、inspector / artifacts / preflight
    - 验证：
      - `test_butler_flow.py`
      - `test_butler_cli.py`
      - `test_chat_cli_runner.py`
      - `test_codex_provider_failover.py`
      - `tools/butler-flow --help`
    - `tools/butler-flow preflight`
    - `python -m butler_main --help`
    - Textual import smoke
17. 再补一轮 **butler-flow TUI 信息架构重排**：
    - 现役 TUI 从“左侧常驻 flow list + 右侧 transcript / inspector”改成“单 flow 主视图 + `/history` `/settings` 整屏模式`
    - 单 flow 主视图固定为：
      - 左栏：当前状态 / goal / guard / last judge / last operator / phase step history
      - 右栏：可滚动 transcript
    - 历史 flow 选择不再常驻左栏，统一通过 `/history` 进入 history screen
    - 当前 `/history` 已进一步收口为：
      - 左栏顶部头部样式 summary + 可选 flow list
      - 右栏摘要 + latest signals + recent step 简表
      - 高亮只更新右栏，`Enter` 才切回 single flow
    - TUI 偏好统一通过 `/settings` 进入 settings screen；当前只管理 session-scoped 的 follow / runtime event / transcript filter
    - 新增 slash commands：
      - `/history`
      - `/settings`
      - `/back`
      - `/focus <flow_id>`
      - `/follow [on|off]`
      - `/filter <all|assistant|system|judge|operator>`
      - `/runtime-events [on|off]`
    - 验证：
      - `test_butler_flow.py`
      - `test_butler_flow_tui_controller.py`
      - `test_butler_flow_tui_app.py`
18. 再补一条 **butler-flow exec 测试入口**：
    - 新增 `butler-flow exec run`
    - 新增 `butler-flow exec resume`
    - 固定不进入 TUI，stdout 全量输出 JSONL
    - 最后一行固定输出 `kind=flow_exec_receipt`
    - 退出码按 flow 终态收口：`completed -> 0`、`failed -> 1`、`interrupted -> 130`
    - 验证：
      - `test_butler_flow.py`
      - `tools/butler-flow --help`
      - `python -m butler_main.butler_flow --help`
19. 再补一轮 **supervisor fix 自治能力**：
    - `supervisor` 不亲自执行 repo 修复，只决定是否进入显式 `fix` turn
    - `fix` 默认复用同一 Codex session，由主 executor 执行
    - judge 新增 `issue_kind / followup_kind`，把 `agent_cli_fault`、普通 bug、plan gap、service fault 分开
    - `fix` 已收紧为只处理本地 agent CLI 调用链故障；普通 bug 继续走普通 `execute/retry`
    - Codex timeout / provider / 网络 / 429 / 5xx 等服务类故障不进入 `fix`
    - 连续 `agent_cli_fault` auto-fix 超过 `2` 轮后，flow 自动转 `paused + operator_required`
    - 验证：
      - `test_butler_flow.py`
19A. 当日末再补一轮 **butler-flow 1.1.0 flow 管理面**：
    - 新增 `butler-flow flows`
    - 新增 `butler-flow flows --manage <new|last|flow_id>`
    - 新增 `managed_flow` workflow kind，支持 ordered phase plan 与动态 phase 切换
    - 新增 `manage_agent.py`，由 Cursor 侧 manager agent 交接 `goal / guard_condition / phase_plan / risk_level / autonomy_profile / operator_guidance`
    - 每条 flow 新增 `flow_definition.json` sidecar，作为 flow 编排与管理真源
    - Textual TUI 新增 `/flows` 与 `/manage`
    - 版本号更新为 `1.1.0`
    - 验证：
      - `./.venv/bin/python -m pytest butler_main/butler_bot_code/tests/test_butler_flow.py butler_main/butler_bot_code/tests/test_butler_flow_tui_controller.py butler_main/butler_bot_code/tests/test_butler_flow_tui_app.py butler_main/butler_bot_code/tests/test_butler_cli.py -q`
      - 结果：`64 passed`
20. 再补一轮 **仓库根目录归档整理收口**：
    - 专题正文：[05_根目录归档整理收口.md](./05_根目录归档整理收口.md)
    - 当前裁决：
      - `runtime_os/` 保持为根目录现役兼容命名空间，不做下沉或归档
      - `tools/` 保持为根目录现役入口，并新增 `tools/README.md`
      - 根目录历史快照 `1/`、`C:/` 迁入 `过时/20260331_root_snapshots/`
      - 根目录临时单文件 `dir_names_utf8.txt` 迁入 `过时/20260331_root_misc/`
      - `MyWorkSpace/` 标记为待收敛旧并行工作台，不再作为新增默认落位
    - 验证：
      - `import runtime_os / runtime_os.agent_runtime / runtime_os.process_runtime`
      - `tools/butler --help`
      - `tools/butler-flow --help`
20. 再补一轮 **visual console 与 0331 agent_turn 主线对齐复核**：
    - `console` 后端投影从旧相位链收口为：
      - `ledger`
      - `turn`
      - `delivery`
      - `harness`
    - campaign-alone `append_feedback` 已补齐，不再强依赖 `mission_id`
    - operator 主动作收口为：
      - `pause`
      - `resume`
      - `abort`
      - `annotate_governance`
      - `force_recover_from_snapshot`
      - `append_feedback`
    - 旧 `force_transition / skip_to_step` 降为 legacy/best-effort
    - 前端 `Operator Harness` 主界面改为自然语言 `task_summary + latest_turn_receipt + delivery refs + harness health + audit feed`
    - 已回写：
      - [0330 后台任务操作面与多Agent编排控制台升级计划](../0330/01_后台任务操作面与多Agent编排控制台升级计划_未实施草稿版.md)
      - [0331 后台主线 Campaign 宏账本与 Agent 可重入内环实施回写](./03_后台主线控制面瘦身与Agent内环提权草稿计划.md)
      - [Visual Console API Contract v1](../../runtime/Visual_Console_API_Contract_v1.md)
    - 验证：
      - `./.venv/bin/python -m pytest butler_main/butler_bot_code/tests/test_console_services.py butler_main/butler_bot_code/tests/test_console_server.py butler_main/butler_bot_code/tests/test_orchestrator_campaign_observe.py butler_main/butler_bot_code/tests/test_orchestrator_feedback_notifier.py -q`
      - `cd butler_main/console/webapp && npm run typecheck`
      - `cd butler_main/console/webapp && npm run build`
20. 再补一轮 **visual console 前端展示修复**：
    - 症状：
      - inspector / preview / agent detail 允许同时打开，出现窗口堆叠
      - 日夜模式虽有状态切换，但首屏与部分 overlay/sheet 没有真正跟随 dark theme
    - 修复：
      - store 与 `App.tsx` 收口为 overlay 互斥：preview/detail 打开时会清掉其它弹层，inspector 打开时也会清理 preview/detail
      - `index.html` 在 hydrate 前预写 `data-theme`，避免首屏闪回 light
      - `styles.css` 增补主题 token：command rail、sheet、overlay backdrop、code surface、preview pane 等全部跟随 theme
      - console bundle 已重建并重启 console 服务
    - 验证：
      - `cd butler_main/console/webapp && npm test`
      - `cd butler_main/console/webapp && npm run typecheck`
      - `cd butler_main/console/webapp && npm run build`
      - `./tools/butler restart console`
      - `./tools/butler status`
21. 再补一轮 **visual console 主题系统与左 rail 适配修复**：
    - 症状：
      - light theme 下 graph/workspace 仍是黑底，部分文本保留浅色导致看不清
      - dark theme 下 flow node 等局部又会落成浅底浅字
      - 左侧 rail 在展开/折叠切换后存在滚动和挤字适配问题
    - 修复：
      - `styles.css` 新增 stage / graph / flow node / timeline / activity dock 主题 token，移除固定深底浅字假设
      - light theme 统一为浅工作面 + 深字；dark theme 保持深场景，并补齐 flow node 的 dark token
      - `command-rail` 与 `console-root` 收口为可滚动 rail、加宽 collapsed width，并缩短 collapsed 按钮文案
    - 验证：
      - `cd butler_main/console/webapp && npm test`
      - `cd butler_main/console/webapp && npm run typecheck`
      - `cd butler_main/console/webapp && npm run build`
      - `./tools/butler restart console`
      - `./tools/butler status`
22. 再补一轮 **前台 `butler-flow` role runtime / role-session 绑定实施回写**：
    - 正文：[06_前台butler-flow角色运行时与role-session绑定计划.md](./06_前台butler-flow角色运行时与role-session绑定计划.md)
    - 当前裁决：
      - `simple` 继续单 session
      - `medium` 已落 role-bound session
      - `complex` 当前只保留 `per_activation` 合同
    - 已落代码：
      - `execution_mode / session_strategy / active_role_id / role_pack_id`
      - `role_sessions.json`
      - `handoffs.jsonl`
      - `artifacts.json` 的 role visibility 字段
      - `role_packs/{coding_flow,research_flow}`
    - 验证：
      - `test_butler_flow.py`
      - `test_butler_cli.py`
      - `test_butler_flow_tui_controller.py`
      - `test_butler_flow_tui_app.py`
      - `test_chat_cli_runner.py`
    - 版本号更新为 `1.2.0`
    - 同步回写：
      - `docs/README.md`
      - `project-map/02_feature_map.md`
      - `project-map/03_truth_matrix.md`
      - `project-map/04_change_packets.md`

## 影响范围

- 影响 `butler_main/agents_os/execution/`、`butler_main/runtime_os/agent_runtime/__init__.py`、`tools/butler` 与对应测试。
- 夜间这轮补丁额外影响 `butler_main/butler_cli.py`、`butler_main/butler_bot_code/configs/butler_bot.json`、`test_workflow_shell.py` 与 `test_butler_cli.py`。
- 系统级 CLI 入口补丁额外影响 `butler_main/__main__.py`、`tools/install-butler-flow` 与 `test_butler_cli.py`。
- 前台 workflow shell 不进入 `campaign/orchestrator` 后台任务主链；前台 shell 与 `/bg -> campaign/orchestrator` 真源边界继续硬切开。
- 当日还包含一条 Butler 主干运行入口修复，但未改变现役分层裁决。
- 当日还包含一轮 `0330/02` 的 docs-only 真源拆分，影响 Agent Harness 开发入口与 project-map 导航，但不改代码行为。
- 当日晚些时候还包含一条 visual console 访问修复，影响 `butler_main/console/`、`butler_main/console/webapp/` 与 `test_console_server.py`，不改 API 语义，只修正静态资源可达性。
- 当日晚些时候又把后台主线架构瘦身方向落实为实际代码，影响 `butler_main/domains/campaign/`、`butler_main/orchestrator/interfaces/`、`butler_main/orchestrator/feedback_notifier.py` 与对应测试，并同步回写 `docs/README.md`、`project-map/02_feature_map.md`、`project-map/03_truth_matrix.md`、`project-map/04_change_packets.md`。
- 当日晚些时候还补一份 docs-only 草稿计划，聚焦前台长 workflow 的产品化方向，当前不改代码行为，也不改 project-map 现役裁决。
- 当日最后再把前台 `butler-flow` role runtime / role-session 绑定从草稿落实为代码与真源，影响 `butler_main/butler_flow/`、`butler_main/butler_flow/role_packs/`、`test_butler_flow.py`、`test_butler_cli.py`、`test_butler_flow_tui_controller.py`、`test_butler_flow_tui_app.py`、`test_chat_cli_runner.py`，并回写 `docs/README.md`、`project-map/02_feature_map.md`、`project-map/03_truth_matrix.md`、`project-map/04_change_packets.md`。
- 当日最后又补一份 docs-only 主计划，聚焦 `butler-flow` 的 Textual-first CLI 完备升级与视觉设计，影响 `docs/README.md`、`project-map/03_truth_matrix.md`、`project-map/04_change_packets.md` 与当日总纲，但不改代码行为。
- 当日最后再把这份 04c 主计划落实为实际代码，影响 `butler_main/butler_flow/`、新增 `requirements-cli.txt`，并更新当前真源与导航文档。
- 当日最后再补一轮 docs-only 根目录治理实施回写，影响 `README.md`、`docs/README.md`、`tools/README.md`、`project-map/02_feature_map.md`、`project-map/03_truth_matrix.md`、`project-map/04_change_packets.md` 与 `过时/` 下新增归档说明文件。
- 当日最后又补一轮 visual console 与 0331 `agent_turn` 主线对齐，影响 `butler_main/console/`、`butler_main/console/webapp/spa/`、`butler_main/domains/campaign/service.py`、`butler_main/orchestrator/interfaces/campaign_service.py` 与对应 console / observe / feedback 测试，同时回写 `0330/01`、`0331/03`、`docs/runtime/Visual_Console_API_Contract_v1.md`、`project-map/02_feature_map.md`、`project-map/03_truth_matrix.md`、`project-map/04_change_packets.md`、`docs/README.md`。
- 当日最后再补一轮 visual console 前端展示修复，影响 `butler_main/console/webapp/spa/src/App.tsx`、`state/console-store.ts`、`state/console-store.test.ts`、`styles.css`、`index.html` 与 `0330/01`、`project-map/03_truth_matrix.md`、本当日总纲；不改 API 合同，只收口前端交互与主题表现。
- 当日最后再补一轮 visual console 主题系统与左 rail 适配修复，影响 `butler_main/console/webapp/spa/src/styles.css`、`components/command-rail.tsx` 与 `0330/01`、`project-map/03_truth_matrix.md`、本当日总纲；不改 API 合同，只收口主题 token、graph/flow node 对比度和 rail 布局。
- 与 [0325 Codex 原生能力边界](../0325/10_Codex原生能力边界与Butler吸收参考.md) 互补：
  - `01_...` 偏 **CLI 集成与监管**
  - `02_...` 偏 **Butler 前台 workflow shell 实现与边界**

## 0331 改动范围交叉核对

### 核对方法

- 非 git 场景下，本轮以 **文档真源 + 文件修改时间** 双轨交叉核对：
  - 文档真源：`README.md`、`docs/README.md`、`docs/daily-upgrade/0331/00_当日总纲.md`，以及 `0331/01`、`02`、`03`、`04b`、`04c`、`05`。
  - 文件时间：扫描 `butler_main/`、`tools/`、`docs/`，并补看仓库根当日文件。
- 文件时间统计固定限定在 `2026-03-31 00:00:00 ~ 2026-04-01 00:00:00`。
- 统计时排除明显运行态/构建态噪音：`__pycache__/`、`node_modules/`、`dist/`、`build/`、`run/`、`sessions/`、`plugins/`。
- 额外识别出运行产物但不计入“核心改动总结”：`butler_main/butler_bot_code/logs/` 与 `butler_main/chat/data/hot/recent_memory/`。

### 核对结果

- 当日扫描命中 `115` 个文件，其中：
  - `102` 个为可归入代码 / 测试 / 文档的有效改动文件
  - `13` 个为运行产物（`2` 个日志、`11` 个 recent-memory scope 热数据）
- 仓库根额外命中：
  - `README.md`
  - `requirements-cli.txt`
  - `.env.codex.local`
- 其中 `.env.codex.local` 属本机环境侧文件，本总结不把它当作仓库产品改动真源。
- 有效改动按主题分组后，与当日总纲裁决基本一致：
  - `butler-flow / system cli`：`20` 个文件
  - `campaign / orchestrator / feedback`：`9` 个文件
  - `visual console`：`18` 个文件
  - `runtime entry / circular import fix`：`8` 个文件
  - `execution runtime bridge`：`6` 个文件
  - `tests`：`13` 个文件
  - `docs / navigation`：`25` 个文件

## 结构化总结

### 1. 改动主题：前台 `butler-flow` CLI 产品化与系统级入口收口

- 主要模块：`butler_main/butler_flow/`、`butler_main/butler_cli.py`、`butler_main/__main__.py`、`tools/butler-flow`、`tools/install-butler-flow`、`requirements-cli.txt`
- 关键文件：
  - `butler_main/butler_flow/cli.py`
  - `butler_main/butler_flow/runtime.py`
  - `butler_main/butler_flow/events.py`
  - `butler_main/butler_flow/tui/app.py`
  - `butler_main/butler_flow/tui/controller.py`
  - `tools/butler-flow`
  - `tools/install-butler-flow`
- 主要影响：
  - 前台 `workflow shell` 正式收口为 `butler-flow`
  - 新增 `exec run` / `exec resume`、`action`、`tui`、`--plain` 等执行面
  - 新增 Textual-first 前台产品壳与结构化事件脊柱
  - 新增系统级 CLI 安装入口与 `python -m butler_main` 顶层入口

### 2. 改动主题：后台 `campaign` 主线瘦身与 query/feedback 收口

- 主要模块：`butler_main/domains/campaign/`、`butler_main/orchestrator/interfaces/`、`butler_main/orchestrator/feedback_notifier.py`
- 关键文件：
  - `butler_main/domains/campaign/service.py`
  - `butler_main/domains/campaign/status_semantics.py`
  - `butler_main/domains/campaign/store.py`
  - `butler_main/orchestrator/interfaces/campaign_service.py`
  - `butler_main/orchestrator/interfaces/query_service.py`
  - `butler_main/orchestrator/feedback_notifier.py`
- 主要影响：
  - 新主线从旧 phase/mission 语义收口到 `campaign 宏账本 -> workflow_session -> latest_turn_receipt/task_summary`
  - `resume_campaign` 更接近“一次可重入 supervisor turn”
  - `feedback/query` 优先读取宏摘要与最新 receipt，而不是继续把旧兼容语义当首真源

### 3. 改动主题：`visual console` 可达性、展示与主题系统修复

- 主要模块：`butler_main/console/`、`butler_main/console/webapp/spa/`
- 关键文件：
  - `butler_main/console/server.py`
  - `butler_main/console/service.py`
  - `butler_main/console/webapp/spa/vite.config.ts`
  - `butler_main/console/webapp/spa/src/App.tsx`
  - `butler_main/console/webapp/spa/src/state/console-store.ts`
  - `butler_main/console/webapp/spa/src/styles.css`
  - `butler_main/console/webapp/spa/src/components/command-rail.tsx`
- 主要影响：
  - 修正 `/console/` 与 `/console/assets/*` 的静态资源可达性
  - 前端展示改为对齐 0331 `agent_turn` 主线
  - 主题 token、graph/flow node 对比度、left rail 布局继续收口

### 4. 改动主题：运行入口稳定性修复与执行桥接补丁

- 主要模块：`butler_main/chat/`、`butler_main/butler_bot_code/manager.py`、`butler_main/agents_os/execution/`
- 关键文件：
  - `butler_main/chat/core_defaults.py`
  - `butler_main/chat/core_client.py`
  - `butler_main/chat/config_runtime.py`
  - `butler_main/butler_bot_code/manager.py`
  - `butler_main/agents_os/execution/workflow_shell.py`
  - `butler_main/runtime_os/agent_runtime/__init__.py`
- 主要影响：
  - 消除了 `tools/butler status` 对应链路的循环导入风险
  - 把 chat/runtime/execution 的依赖边界压回轻量导入面
  - 为 `butler-flow` 与现有执行运行时桥接提供更稳定的入口

### 5. 改动主题：测试与文档导航同步回写

- 主要模块：`butler_main/butler_bot_code/tests/`、`docs/`、`tools/README.md`
- 关键文件：
  - `butler_main/butler_bot_code/tests/test_butler_flow.py`
  - `butler_main/butler_bot_code/tests/test_butler_cli.py`
  - `butler_main/butler_bot_code/tests/test_console_server.py`
  - `butler_main/butler_bot_code/tests/test_console_services.py`
  - `butler_main/butler_bot_code/tests/test_orchestrator_campaign_service.py`
  - `docs/README.md`
  - `docs/project-map/02_feature_map.md`
  - `docs/project-map/03_truth_matrix.md`
  - `docs/project-map/04_change_packets.md`
- 主要影响：
  - 当日改动已有对应回归测试面
  - 文档导航与真源矩阵已同步指向 0331 新主线
  - 根目录治理与工具入口规则补到了正式文档

## 风险与待确认项

- 非 git 场景下，这份总结依赖文件 `mtime`；若某些文件被同日批量重写或二次生成，可能放大“当天改动范围”。
- `butler_main/chat/data/hot/recent_memory/` 与 `butler_main/butler_bot_code/logs/` 明显属于运行态副产物，不应误判为产品代码变更。
- `visual console` 与 `Textual TUI` 的最终质量仍依赖实际运行验证；仅靠文件时间与文档无法替代页面/TUI 可操作性验收。
- 仓库根存在 `.env.codex.local` 当日变更，但它是本机环境配置，不应直接作为跨环境交付依据。
- 若 review 阶段要确认“当日是否还有遗漏主题”，应再用本摘要反查 `README.md`、`docs/README.md`、`0331/00~05` 各专题正文是否一一对应。

## Review 自检清单

- [ ] 已按非 git 方法完成 `2026-03-31` 当日文件范围扫描，并说明排除规则。
- [ ] 已把运行产物与有效改动分开，不把日志 / recent-memory 热数据当作核心代码改动。
- [ ] 已把改动归纳为可复核的主题，而不是只罗列文件名。
- [ ] 每个主题都给出涉及模块、关键文件与主要影响。
- [ ] 已明确写出风险与待确认项，方便 review 阶段追问。
- [ ] 已给出后续优先复核的测试与文档入口。

## 影响的 project-map 条目

- [02_feature_map.md](../../project-map/02_feature_map.md)
- [03_truth_matrix.md](../../project-map/03_truth_matrix.md)
- [04_change_packets.md](../../project-map/04_change_packets.md)
- [README.md](../../README.md)
