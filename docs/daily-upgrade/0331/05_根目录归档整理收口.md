# 0331 根目录归档整理收口

日期：2026-03-31  
状态：已落地  
主层级：docs only / 导航与治理  
对应主题：仓库根目录治理、归档、兼容入口裁决

## 本轮裁决

1. `runtime_os/` 保持为根目录现役兼容命名空间：
   - 用途是承接 `runtime_os.* -> butler_main.runtime_os.*`
   - 当前不做下沉、不做归档、不做重命名
2. `tools/` 保持为根目录现役入口：
   - 承接 `butler`、`butler-flow`、安装脚本、relay 和审计脚本
   - 新增 [`tools/README.md`](../../tools/README.md) 说明分类和维护规则
3. 根目录历史快照对象迁出：
   - `1/` -> `过时/20260331_root_snapshots/1_run_snapshot/`
   - `C:/` -> `过时/20260331_root_snapshots/C_drive_workspace_snapshot/`
4. 根目录临时单文件迁出：
   - `dir_names_utf8.txt` -> `过时/20260331_root_misc/dir_names_utf8.txt`
5. `MyWorkSpace/` 当前定性为待收敛旧并行工作台：
   - 不再作为新增内容默认落位
   - 现役工作台继续以 `工作区/` 为准

## 判定依据

- `runtime_os/` 目录内文件是 repo-root compatibility surface，直接 import 到 `butler_main.runtime_os.*`
- `tools/butler`、`tools/butler-flow`、`tools/install-butler-flow` 均是现役启动入口
- 根目录 `1/` 仅保留一次性运行态空壳目录
- 根目录 `C:/` 是历史跨环境 `workspace` / `工作区` 交付快照，不属于 Linux 根目录现役结构
- `dir_names_utf8.txt` 未命中现役代码依赖，只在历史 worker log 中出现

## 本轮落地动作

1. 给 `tools/` 补说明文件，固定脚本分类与维护边界
2. 给归档快照目录补 `README.md`，记录来源、迁移日期、只读属性和替代入口
3. 更新仓库根 `README.md`，明确根目录保留清单和本轮已迁对象
4. 更新 `docs/README.md`、`project-map/02_feature_map.md`、`03_truth_matrix.md`、`04_change_packets.md`，让 docs-only 改动能命中本专题
5. 更新 `00_当日总纲.md` 回写本轮归档治理

## 当前根目录口径

根目录长期保留：

- `butler_main/`
- `docs/`
- `工作区/`
- `过时/`
- `runtime_os/`
- `tools/`
- 仓库级入口文件与本地开发环境目录

根目录长期不保留：

- 数字目录
- 盘符模拟目录
- 单次交付快照
- 一次性 `.txt/.md/.json` 临时文件
- 与 `工作区/` 平行的第二套新增活跃工作区

## 验收

- `python3 -c "import runtime_os, runtime_os.agent_runtime, runtime_os.process_runtime"` 成功
- `tools/butler --help` 成功
- `tools/butler-flow --help` 成功
- 根目录不再存在 `1/`、`C:/`、`dir_names_utf8.txt`
- 新归档目录均带说明文件
