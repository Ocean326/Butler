---
description: 改动优先级（skills/文档/json >> 代码）与临时文件放置规范
alwaysApply: true
---

# 改动优先级与临时文件约定

## 改动优先级（从高到低）

- **优先改 skills / 文档 / 配置**
  - 优先通过 `./butler_main/butler_bot_agent/skills` 下的 skill、说明文档、JSON/配置文件来完成需求或调整行为。
  - 能用 skill / 文档 / 配置解决的，不优先直接改核心代码或核心 prompt。

- **其次才改核心代码**
  - 仅在 skills / 文档 / 配置无法满足需求、或已经明确需要结构性调整时，再考虑修改 `butler_bot_code` 等核心代码区域。

- **核心 prompt / SOUL 改动更慎重**
  - 涉及 `feishu-workstation-agent.md`、`Butler_SOUL.md` 以及其他人格/提示词文件时，视为高风险改动，需确保是稳定偏好或多轮沉淀结论。

## 核心代码区与核心 prompt 区

- **保持干净整洁**
  - 核心代码与核心 prompt 区只保留长期可维护的逻辑与人格设定，避免堆叠临时实验、一次性脚本或大段备忘。

- **临时需求先走外围**
  - 与一次性任务、短期实验相关的逻辑，优先放在 skills、工作区文档或临时脚本中，而不是直接堆进核心文件。

## 临时脚本与临时文档

- **默认落在 `工作区/temp`**
  - 对话中需要的临时脚本、一次性分析、草稿文档，默认放在 `./工作区/temp` 目录下。
  - `temp` 视为可清理区：用完可以删除；只有确定需要长期保留或复用时，才从 `temp` 挪到对应工作事务目录。

- **迁移再谈可维护性**
  - 当临时内容被证明有复用价值时，再整理重构，迁移到正式的 `./工作区/<具体项目>` 目录或相应 skill/文档中，确保结构清晰、命名可理解。

